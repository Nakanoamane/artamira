# Test info

- Name: ホームページが正しく表示されること
- Location: /app/frontend/tests/example.spec.ts:3:1

# Error details

```
Error: Timed out 5000ms waiting for expect(locator).toHaveTitle(expected)

Locator: locator(':root')
Expected pattern: /Artamira/
Received string:  ""
Call log:
  - expect.toHaveTitle with timeout 5000ms
  - waiting for locator(':root')
    9 × locator resolved to <html>…</html>
      - unexpected value ""

    at /app/frontend/tests/example.spec.ts:7:22
```

# Page snapshot

```yaml
- text: "Blocked request. This host (\"frontend\") is not allowed. To allow this host, add \"frontend\" to `server.allowedHosts` in vite.config.js."
```

# Test source

```ts
   1 | import { test, expect } from '@playwright/test';
   2 |
   3 | test('ホームページが正しく表示されること', async ({ page }) => {
   4 |   await page.goto('http://frontend:5173/'); // フロントエンドのURL
   5 |
   6 |   // 例: ページのタイトルをチェック
>  7 |   await expect(page).toHaveTitle(/Artamira/);
     |                      ^ Error: Timed out 5000ms waiting for expect(locator).toHaveTitle(expected)
   8 |
   9 |   // 例: 特定のテキストが存在するかチェック
  10 |   await expect(page.locator('h1')).toContainText('Artamira - リアルタイム共同お絵かきボード');
  11 | });
  12 |
```